server: 
        port: 9000

spring:
    application:
        name: FreshVueDB
    datasource:
        url: jdbc:mysql://localhost/FreshVueTestDB?serverTimezone=America/Chicago&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: fanger
    jpa:
        hibernate:
            naming:
                implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    jackson:
        serialization:
            FAIL_ON_EMPTY_BEANS: false
